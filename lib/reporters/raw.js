/**
 * Just returns the raw javascript todos
 * @module raw-reporter
 */

/**
 * @alias module:raw-reporter
 * @param {Object[]} todos - The parsed todos
 * @returns {Object[]}
 */
const reporter = todos => todos;

module.exports = reporter;
